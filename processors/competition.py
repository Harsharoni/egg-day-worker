import pandas as pd

# MER is computed server-side and returned in the JSON response.
# No local calculation needed. This module is a placeholder for any
# additional competition-specific enrichment added later.


def enrich(df: pd.DataFrame) -> pd.DataFrame:
    return df
